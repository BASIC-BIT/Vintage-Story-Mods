# Agent Control Sample

This ordinary client mod references only `agentcontrol.abstractions` and registers `selection.describe` during `StartClientSide`. The operation reports a richer block or entity selection, proving a new native operation can be added without changing the controller core or its six-method RPC surface.
