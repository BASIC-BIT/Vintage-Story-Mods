# Pre-existing complex functions that exceed Lizard thresholds.
# These should be refactored over time (see issue #115).
# Format: function_name at file_path

# CCN=52, 205 lines - needs Phase 5 refactor to extract local functions
PlayerChatTransformer::Transform

# CCN=20, 107 lines - rendering callback with many branches
TypingIndicatorRenderer::OnRenderFrame

# CCN=6 but 154 lines - long command registration (not actually complex)
RPProximityChatSystem::RegisterCommands

# CCN=15, 133 lines - complex request handling with many validation branches
TpaSystem::HandleTpaRequest

# CCN=17, 120 lines - argument parser with many match conditions
PlayerByNameOrNicknameArgParser::TryProcess

# CCN=27, 163 lines - Harmony prefix patch with many conditional branches
SpeechBubbleVtmlPatches::Prefix

# CCN=10 but 108 lines - texture generation setup with resource management
RichTextTextureUtils::GenRichTextTexture

# CCN=24, 101 lines - headshot upload pipeline with validation and image processing branches
CharacterSheetSystem::HandleHeadshotUpload

# CCN=24, 104 lines - character sheet layout composition with optional header/body sections
CharacterSheetDialog::ComposeDialog

# CCN=30, 98 lines - nametag Harmony texture replacement with defensive render branches
EntityBehaviorNameTagPatches::ReplaceTexture

# CCN=11, 107 lines - rich text surface generation with native drawing lifecycle
RichTextTextureUtils::GenRichTextSurface

# CCN=22, 121 lines - chatter dispatch mixes audio selection and emission
RPProximityChatSystem::DispatchChatterForContext

# Lizard treats the admin config registry's file-level setting definitions as one global block.
*global*

# DimensionLib immutable metadata record has a deliberately wide constructor mirroring persisted fields.
Dimension::Dimension

# DimensionLib command registration is long but mostly declarative command tree wiring.
DimensionLibCommandRegistrar::Register

# Network sync method mirrors the transfer packet fields and Vintage Story player state inputs.
DimensionTransferService::SyncClientTransfer

# Cavern spike helper samples several terrain inputs; keep local until a second generator needs it.
CavernBlockSource::AddSpikes
